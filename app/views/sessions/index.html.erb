<h1>Welcome to a place with photos.</h1>

<%= current_user.email %>
<%= form_for current_user, :url => users_path, :html => { :multipart => true } do |form| %>
  <%= form.file_field :avatar %>
<% end %>

<%= image_tag current_user.avatar.url %>
<%= image_tag current_user.avatar.url(:medium) %>
<%= image_tag current_user.avatar.url(:thumb) %>

<a href="/log-out">Not you?  Sign out!</a>
<a href="/users/new">Or make new account</a>
